{
  "name": "chaincheck",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create RESPONSES",
    "kv:create:preview": "wrangler kv namespace create RESPONSES --preview"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
